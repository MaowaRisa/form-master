/**
 * 1. use form tag and onSubmit handler with event (e) From (e) access e.target.[input field name].value 
 * 2. Controlled element: Use individual field state for each input field.
 * 3. controlled element: one object in a state holding all the input fields value.
 * 4. uncontrolled element: useRef to create reference to the element and access value by using like: nameRef.current.value
 * 
 * controlled component and uncontrolled components to handle the form values.
 * 5. use hook to handle states and submit and error. example react hook form.
 * 6. reusable components.
*/